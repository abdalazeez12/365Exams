@model ContentPageViewModel
@await Html.PartialAsync("_Breadcrumbs", Model)
<!-- Start About
    ============================================= -->
    <div class="about-area default-padding">
        <div class="container">
            <div class="row">
                <div class="about-items">
                @for (int i = 0; i < Model.StaticPage.Contents.Count; i++)
                    {
                    <div class="col-md-12 about-info">
                        <h2><span>@Model.StaticPage.Contents[i].LocalizedName</span></h2>
                        <blockquote>
                            @Html.Raw(Model.StaticPage.Contents[i].LocalizedDescription)
                        </blockquote><br>
                    </div>

                    }
               

                </div>
            </div>
        </div>
    </div>
    <!-- End About -->